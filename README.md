# Proposal Portal Astra

정보시스템 감리 제안팀의 인력·사업 조회, 본문 PPT 및 첨부서류 제작 포털입니다.

## 실행 환경과 연결 대상

- GitHub: https://github.com/sweatmoon/proposal-portal-astra
- Astra 운영 주소: https://proposal-portal-proposal-portal-astra.up.railway.app/
- Hono + Node.js + PostgreSQL, Docker/Railway 배포
- 사진·회사 증빙·인력 서류는 Synology NAS에서 조회
- Cloudflare 초기 파일이 남아 있지만 현재 실행 대상은 Node.js입니다.
- 승인된 변경만 Astra GitHub main에 푸시합니다. GitHub 푸시 성공과 Railway 배포 완료는 별개이며, 이번 작업에서 Railway 배포 상태 조회는 제외합니다.
- 운영 DB 및 원본 `sweatmoon/proposal-portal`은 변경하지 않습니다.

## 주요 경로

- `/`: 현황 대시보드
- `/proposals`, `/proposals/:id`: 사업 목록·상세, 본문 PPT 모달
- `/personnel`, `/personnel/:id`: 인력·경력 조회
- `/upload`: HTML 적재
- `/ppt-templates`: 목차·템플릿 관리
- `/ppt-generate`: 첨부서류 생성
- `/api/ppt-menus?category=proposal`: 본문 목차·템플릿 조회
- `/api/ppt-menus/master-templates/active`: 활성 마스터 조회
- `POST /api/personnel/photo-images`: 읽기 전용 사진 일괄 조회, JSON `{ "names": ["이름"] }`, 요청당 1~100명
- `GET /api/personnel/photo-image-by-name?name=이름`: 이름 기반 사진 조회
- `GET /api/personnel/:id/photo-image`: ID 기반 사진 조회
- `/health`: 서버 상태

## 제안작업표 감리 요구사항 표시

- `/proposals/:id` 기본 정보 아래 `감리 요구사항` 영역에서 저장된 요구 단계·요구 감리 일수·요구 투입 공수를 함께 표시합니다.
- `required_phases`, `required_audit_days`, `required_md`를 각각 단계·일·MD 단위로 표시하며 실제 배정 단계/공수로 대신 계산하지 않습니다.
- 값이 없으면 `미입력`, 0이면 `0 단계`/`0 일`/`0 MD`로 구분합니다. PostgreSQL 숫자 문자열과 소수도 표시합니다.
- 기존 금액 정보의 요구투입공수는 이 영역으로 이동했으며 제안투입공수는 그대로 유지합니다.
- 이번 변경은 화면 표시만 추가합니다. HTML 파싱·DB 스키마·저장값 및 PPT 생성 데이터는 변경하지 않습니다.

## 본문 PPT 기능

- `public/static/proposal-template.js`: 본문 전용 XML 치환·계산·검증
- 분산 런·반복 토큰·XML 특수문자·원본 서식 처리
- 사업명·주관기관·대상사업명·기간 전달, 인력별 배정 MD 합산
- 공통/일정의 `[요구감리일수]`는 화면과 같은 저장 요구값(`required_audit_days` → `requestAuditDays`)을 숫자만 치환합니다. `[요구감리일수]일`은 저장값 5일일 때 `5일`로 표시하며 실제 단계 일수나 고정 5로 대체하지 않습니다. 공백·분산 런 토큰, 숫자 문자열·소수·0을 지원하고 누락·음수·비숫자는 미치환 경고로 남깁니다. 3.6의 기존 미입력 표시 정책은 유지합니다.
- 제목·주관기관만 사용하는 일반 장표에 공수 경고를 붙이지 않음. 실제 공수표/공수 토큰에만 적용
- 감리 일정 계획: 단계·분야 행, 전체 기간의 월 열·연도 병합, 날짜 막대를 동적으로 생성
- 세부 감리 일정: 단계별 활동 블록과 상주/상시·검수지원 블록을 실제 개수대로 복제
- 일정표의 표시 글자 크기는 원본 유지. 자동 축소와 페이지 추가 없음. 여백·들여쓰기·표 폭·행 높이·위치를 조정하며 빈 셀의 비표시 문단도 정리
- 좁은 세부 일정 요약 도식에는 종류별 단계 수를 표시. 전체 단계명·날짜는 왼쪽 본표에서 유지
- 대상사업 기간이 `2026.09` 같은 월 단위이면 그대로 치환. 월 경계는 일정 도식 배치에만 사용하고 표시 날짜에 임의 일자를 추가하지 않음
- 조치확인 표: `post > 0`인 인력이 있는 단계만 열로 생성. 인력은 실제 참여자 수만큼 행을 생성하며 3단계/15명 제한 제거
- 단계별 참여 이름·post 합계, 수행방안 및 활동/시기 병합 셀을 새 행·열 개수에 맞춤. 대상이 없으면 대상 없음 표시
- 조치확인 장표의 투입 공수는 해당 단계 참여자의 `post` 합계입니다. 전체 `pre+audit+post` 합계나 사업 제안공수와 비교하지 않으며, `ACTION_CONFIRM_STAFF`에는 사업 전체 제안공수 불일치 경고를 전달하지 않습니다. 잘못된 배정값·인력 목록 누락·미치환 및 수행방안/횟수 검토 경고는 유지하고, 다른 전체 공수 검증은 변경하지 않습니다.
- 조치확인 주요 수행방안은 원본 셀을 복제하고 병합 범위만 조정. `txBody`/`tcPr`를 보존하여 색상·강조·글머리표·명시적 줄바꿈·빈 문단·들여쓰기·셀 여백을 평탄화하지 않음
- 조치확인 왼쪽 분류는 실제 분야 기준: 사업관리/품질보증 → 응용시스템(하위 분야) → 데이터베이스 → 시스템 구조 및 보안. 응용시스템 상위 셀은 세로 병합, 나머지 분야는 두 분류 칸을 가로 병합. 실제 분야명과 괄호의 사업 구분은 유지
- 분야 판별은 사업관리/품질보증·DB/데이터베이스·시스템구조 등 명시적 명칭 우선. 나머지 감리원 분야는 응용시스템 하위로 표시하며, 그 외 전문가는 해당 분야명을 그대로 표시. 분야 누락은 확인 필요로 표시. 분류 내 기존 인력 순서와 단계별 이름 대응은 유지
- 일부 목차 실패나 경고가 있어도 생성된 장표는 확인창 없이 다운로드. 전부 실패/합본 실패는 오류 표시
- 사진 장표의 기존 2/4/6/9인 슬롯·인력 순서·페이지 복제 유지
- 3.1/3.2/3.3 공용 사진 생성기: 이미지 관계 ID는 한 번만 치환. 공유 rId의 첫 객체를 임의로 바꾸는 fallback을 제거하고 선택된 사진 DOM 객체만 교체
- 장표 밖 우측·하단 예시 그림은 슬롯 귀속 및 빈 슬롯 삭제에서 제외. 사진은 장표 안 이름 플레이스홀더에 인접한 단일 증명사진 후보만 선택
- 사진 객체 누락·중복이나 좌표가 불확실한 그룹 내부 사진은 다른 로고로 대체하지 않음. 기존 사진 없음/TBD 기본 이미지 정책은 유지. 새 양식을 등록할 때에는 이름-사진 배치 및 그룹 구조를 검증해야 함
- RFP 공수는 전체 인력 합계, 일수는 각 기본 일반단계별 최소 기준으로 자동 비교. 요구값·배정값 누락 시 충족을 확정하지 않으며 인력 자격은 증빙 검토 필요
- 합본의 미디어 ContentType·외부 링크 보존, 마스터 ID 충돌 방지
- 3.4 감리원·3.5 전문가 실적 장표: 삭제 전에 원본 슬라이드 `.rels`를 확보하고 모든 복제 페이지에 그대로 사용. 그림의 원본 rId·외부 링크 TargetMode·레이아웃 연결 및 우측 작업용 예시 객체를 보존
- 실적 템플릿 관계 파일이 없으면 명시적 오류 처리. 존재 여부를 확인하지 않은 slideLayout6으로 대체하지 않음. 본문 데이터 치환과 2인/4인 페이지 구성은 유지

## 3.6 주관기관 요청사항 준수 여부

- 최신 연결 기준은 사용자가 직접 수정한 `3.6 주관기관 요청사항 준수 여부 (3).pptx`(51,894 bytes, SHA256 `925b198bcaf4661c87890baf6b2c11d4397dd7453bcf7b947f19ac4c2bbd13c8`)입니다. `artifacts/3.6_user_revised_template.pptx`는 이 원본을 변경 없이 보관한 회귀 테스트용 파일입니다. 운영 등록 파일을 자동 변경하지 않습니다.
- `public/static/compliance-proposal-template.pptx`는 이전 완성본을 바탕으로 제작한 선택적 참고양식일 뿐 최신 사용자 수정본이 아닙니다. 기존 다운로드 경로는 유지하지만 이 파일로 재등록하도록 강제하지 않습니다. `compliance-template.pptx` 역시 호환성 검증용입니다.
- 마스터는 사용자 요청에 따라 수정·재디자인 범위에서 제외했습니다. 이전 참고양식 제작 시 `slide1.xml`만 변경하며 마스터·레이아웃·미디어·관계 part는 원본과 바이트 단위로 동일합니다. 재생성 스크립트는 `artifacts/build_compliance_proposal_template.py SOURCE.pptx OUTPUT.pptx`입니다.
- 원본 1장·4열·5개 요청항목, 글꼴 크기·색상·문단·표 위치·행 높이·이미지 관계를 유지합니다. 슬라이드 XML 외 ZIP part는 사용자 원본과 바이트 단위로 동일합니다.
- 이전 참고양식 제작 시 투명 글자 윤곽선(alpha=0) 때문에 LibreOffice에서 판정·상단 문구까지 사라지는 호환성 문제는 동일한 시각 의미의 `a:noFill`로 정규화했습니다. 최신 사용자 수정본에는 이 제작 절차를 적용하지 않았습니다. 글꼴·글자색은 변경하지 않았습니다.
- 우측/하단 장표 밖 참고 표와 작업용 객체는 완성본과 생성 결과 모두 보존합니다. 해당 예시는 실제 사업 데이터 치환 대상이 아니므로 제출 전 편집 화면에서 확인하세요.
- `COMPLIANCE` 및 구버전 `SUMMARY_TABLE`은 등록된 DEFAULT(없으면 등록된 첫 유효) PPTX를 읽습니다. 기존 별도 4항목 PptxGenJS 표 생성은 제거했습니다. 양식 미등록/여러 장 양식은 명시적으로 실패하며 조용히 대체하지 않습니다. 필수 토큰 목록은 없으며 등록된 양식에 존재하는 플레이스홀더만 치환합니다.
- DB 자동 등록은 하지 않습니다. **사용자가 `/ppt-templates`의 해당 3.6 목차 DEFAULT에 등록한 파일을 기준으로 사용합니다.** 코드에 포함된 파일은 선택적인 참고 양식이며 자동 fallback이나 필수 재등록 대상이 아닙니다.
- 플레이스홀더가 없는 고정 문구·판정 칸·배지는 의도된 원문으로 보존합니다. 누락한 토큰을 요구하거나 행 제목/문구를 추정하여 자동 판정으로 덮어쓰지 않습니다. 실제 존재하는 미지원 토큰은 그대로 남기고 미치환 경고를 표시합니다.
- 사용자 확정 기준을 자동 적용합니다. **요구 MD는 감리원·전문가·테스터 전체 배정 공수(추가 단계 포함) 합계와 비교하고, 요구 일수는 각 기본 일반단계가 모두 충족해야 합니다.** 단계 일수의 합계로 대체하지 않습니다. 공수/일수 선택창은 제거하고 `#proposal-comparison-basis`에 저장된 사업별 요구값과 적용 기준을 표시합니다.
- 151MD·5일을 상수로 넣지 않습니다. 사업별 `requestMD`·`requestAuditDays`를 그대로 사용하고, 구형 `mdScope`·`dayScope` 옵션이나 남은 DOM 선택값으로 확정 기준이 변경되지 않도록 했습니다. 일수가 누락·음수·비숫자이면 검토 필요, 확인된 어느 단계라도 요구 미만(0일 포함)이면 미충족입니다.
- 수행 PM 선택은 유지합니다. 선택은 생성 시 브라우저 입력이며 DB에 저장하지 않습니다.
- 숫자 비교용 기본 단계는 추가 선택 단계 및 상시/상주/검수지원 제외입니다. 일정 목록은 보조·추가 단계를 포함해 모두 표시합니다. 단계 명칭/감리 방법의 RFP 대응 여부는 담당자가 확인해야 합니다.
- 전체·기본/추가 감리원·전문가·테스터 MD는 실제 배정값으로 계산합니다. 요구 대비 비율도 전체 인력 MD 합계로 계산합니다. 원본 누락 값(null→0), 잘못된 배정값, 저장 제안 MD 불일치, 배정 없음은 검토 필요입니다.
- `fulltimeKnown`과 `mdComplete`를 SSR JSON에 추가하여 미확정 상근값 및 원본 공수 누락을 구별합니다. 기존 숫자와 사진용 residency 표시를 바꾸거나 DB/파서를 변경하지 않습니다.
- 감리원 인원·수석 표기·상근 비율·분야, 핵심/필수/보안/기타 전문가 및 테스트 인원·분야는 양의 유효 공수가 있는 배정 인력을 중복 제거해 표시합니다. 상근 여부 미확인 시 비율을 확정하지 않습니다.
- 저장된 `director/pmName`이나 첫 번째 인력을 자동 PM으로 정하지 않습니다. 생성 화면에서 선택한 실제 배정 감리원의 이름·저장 등급·상근·자격번호만 반영합니다.
- 총괄 수행 건수·감리 경력·전체 감리 건수는 선택한 PM의 인력 DB에서 자동 조회합니다. `GET /api/personnel/:id/compliance-profile?projectId=N`은 해당 사업과 인력 ID 연결을 확인한 뒤 `personnel` 및 `personnel_audit_history`를 SELECT만 합니다. NAS/운영 DB 쓰기/마이그레이션은 없습니다.
- 총괄 건수는 감리이력 `role`의 총괄·총괄감리원·감리총괄·총괄책임자·PM 등 명시된 역할을 집계합니다. 부총괄/비총괄/총괄보조는 포함하지 않습니다. 전체 건수는 인력정보와 같은 이력 행 수이며 사업명으로 중복 제거하지 않습니다. 역할 누락은 보고서에 알립니다.
- 감리 경력은 최초 감리 연월(없으면 저장된 `auditor_start_date`)에서 현재까지의 년·개월로 표시합니다. `총괄 수행 19건, 감리 경력 6년 2개월 / 141건` 형식이며 숫자를 상수로 넣지 않습니다. 경과기간은 실제 현장 투입 기간 합계와 구별하고 법적 요건의 충족으로 단정하지 않습니다.
- DB 주요이력(`career_expert`)은 해당 종합 경력 토큰이 있을 때 등록 순서의 앞 두 항목을 표시하며 생략된 항목이 있으면 보고합니다. 다른 사람의 예시 사업 경험을 재사용하지 않습니다. 인력 미연결·API 실패·응답 ID/이름 불일치는 보고서에 표시하며, 확인된 0건과 조회 실패를 구별합니다. 최신 개별 실적 토큰은 값이 없거나 조회 실패 시 미치환 상태와 경고를 유지합니다.
- 최신 개별 토큰 `[총괄이름]`은 선택한 실제 배정 PM, `[총괄수행건수]`·`[총괄인력감리경력]`·`[총괄인력감리건수]`는 그 PM의 DB 이력에 연결합니다. 옛 종합 경력 토큰이 없어도 개별 토큰을 감지하여 API를 호출하며 실제 0건은 0으로 치환합니다.
- `[감리원번호]`는 `personnel.auditor_cert_no`를 우선 사용하고 비어 있으면 해당 사업 `proposal_members.auditor_cert_no`를 사용합니다. API에 자격번호 필드가 없거나 조회 실패하면 SSR에 이미 전달된 선택 PM의 번호만 사용할 수 있으며, 양쪽 모두 없으면 토큰을 남깁니다.
- `[단계감리팀수]`는 양의 유효 공수가 배정된 감리원의 중복 제거 인원 수입니다. 전문가·미배정 인력은 제외하고 원본 공수 누락/잘못된 배정이 있으면 수를 확정하지 않습니다.
- `[키워드1]`~`[키워드3]`은 사업 `keywords`의 `sort_order, id` 순서에서 빈 값을 제외한 원문 앞 3개입니다. 인력 실적 매칭용 `keyword_mappings`로 대체하지 않습니다. SSR JSON 및 화면 태그를 안전하게 이스케이프하며 PM 미선택과 독립적으로 전달합니다. 키워드 치환은 해당 인력의 실제 수행 경험을 입증하지 않으므로 보고서에 경고합니다.
- 최신 양식의 고정 기관명·`수석 감리원 (상근)`·`100% 상근`·경험 주장·`충족`은 덮어쓰지 않습니다. 최신 원본의 글꼴·색상·표 좌표·장표 밖 객체·미디어/관계 보존을 실양식 테스트로 검증합니다.
- 새 제안 문구 토큰은 전체/감리원/전문가/테스트 공수와 요청 대비 비율, PM 소개/DB 경력, 감리원 구성, 전문가 투입 문장으로 연결했습니다. `총공수`와 `투입 비율`은 원본 빨간색 11pt, 본문은 10pt 및 원본 문단 서식을 유지하며 여러 줄 값은 OOXML 줄바꿈으로 삽입합니다. 검토 안내는 본문에 나열하지 않고 생성 보고서에 남깁니다.
- 총괄 실적 수치의 수동 입력란은 없습니다. 확인된 팀 경험 문구와 확정 교육계획만 선택 입력할 수 있으며 DB에 저장하지 않습니다. 교육계획이 없으면 임의 계획을 작성하지 않습니다.
- 자격·상근·경험·교육 RFP 문구는 사용자 양식에 있는 고정 조건입니다. 판정·요약 플레이스홀더가 있을 때만 자동 계산/검토 결과를 치환합니다. 고정 `충족`·`100% 충족` 문구는 사용자의 의도대로 유지하며 자동 검증 결과로 간주하지 않습니다. 별도의 생성 보고서에 남기는 계산·증빙 검토 경고는 유지합니다.
- 넘치는 텍스트는 삭제/축소/자동 페이지 분할하지 않습니다. 원본 셀 폭·행간에 따른 높이 초과 가능성을 보고서에 남깁니다. 정상 데이터/미입력 데이터의 실제 양식 LibreOffice 렌더링은 각각 1장으로 확인했으며, 25단계 스트레스 테스트에서는 모든 내용을 유지하고 넘침 경고를 검증했습니다. Windows PowerPoint 직접 검수는 아닙니다.
- 최신 사용자 `(3)` 양식의 6단계·여러 전문가 분야 시험 데이터는 치환과 1페이지 생성은 정상이지만, 긴 일정·전문가 문장으로 하단 공통 사항 일부가 장표 밖으로 잘리는 현상을 LibreOffice 렌더링에서 확인했습니다. 이는 미해결 제한입니다. 원본 표 크기·글꼴을 변경하지 않고 넘침 경고를 유지하며, 별도 승인 없이 자동 축소·문구 삭제·재배치하지 않습니다.
- 단독 요약표 생성과 전체 합본 모두 같은 템플릿 생성기를 사용하고 경고가 있어도 생성된 PPT는 다운로드합니다.

주요 토큰(공백은 치환 시 정규화):

| 구역 | 토큰 |
|---|---|
| 제목·요약 | `[제목]`, `[준수요약]`, `[추가제안요약]` |
| RFP 숫자 | `[요구 단계]`, `[요구 감리 일수]`, `[요구 투입 공수]` |
| 단계·일수 | `[단계구분]`, `[단계별감리일정]`, `[일수비교기준]`, `[방법일수판정]` |
| MD | `[공수합계]`, `[기본감리공수]`, `[추가공수]`, `[전문가공수]`, `[테스트공수]`, `[공수비교내역]`, `[공수판정]` |
| 총괄 | `[총괄감리원]`, `[총괄자격]`, `[총괄경력]`, `[총괄검토사항]`, `[총괄판정]` |
| 감리원 | `[감리원구성]`, `[감리원분야]`, `[감리원검토사항]`, `[감리원판정]` |
| 공통 | `[전문가구성]`, `[교육계획]`, `[공통판정]` |
| 새 일정 문장 | `[제안감리구성]`, `[제안추가감리]`, `[제안감리일정]` |
| 새 공수 문장 | `[제안총공수]`, `[제안투입비율]`, `[제안감리원공수]`, `[제안감리원배치]`, `[제안전문가공수]`, `[제안테스트공수]` |
| 새 인력 문장 | `[제안PM소개]`, `[제안PM경력]`, `[제안감리원구성]`, `[제안감리원경험]`, `[제안전문가구성]` |
| 사용자 수정 PM | `[총괄이름]`, `[감리원번호]`, `[총괄수행건수]`, `[총괄인력감리경력]`, `[총괄인력감리건수]` |
| 사용자 수정 팀·키워드 | `[단계감리팀수]`, `[키워드1]`, `[키워드2]`, `[키워드3]` |

## 3.7 감리원 및 전문가 투입 비율

- `MANPOWER_RATIO`를 전용 등록 양식 생성기에 연결했습니다. DEFAULT(없으면 첫 유효) PPTX를 사용하며, 등록 파일 자체를 변경하거나 참고 양식으로 대체하지 않습니다. 원본 한 장 구조를 사용하고 양식 없음/잘못된 슬라이드 구조는 명시적으로 실패합니다.
- `[단계1]`부터 단계 순서대로 `[A1]` 감리원 MD, `[B1]` 전문가·테스터 MD, `[C1]` 전체 MD를 채웁니다. 각 인력의 `pre+audit+post`를 합산하며 캐시된 그룹 total이나 사업 제안 MD로 대체하지 않습니다. 추가·상시·상주·검수지원 단계도 포함합니다.
- `[A]`, `[B]`, `[C]`는 전체 단계의 각 합계, `[PA]`는 A/C, `[PB]`는 B/C의 백분율입니다. `%`는 양식에 남겨 숫자만 넣고 소수 둘째 자리까지 표시합니다. 전문가 버킷에 테스터가 포함되는 현재 데이터 구조를 보고서에 명시하며 법적 전문가 인정 범위는 별도 검토합니다.
- 빈 인력 버킷은 0MD, 원본 누락(`mdComplete=false` 포함)·음수·비숫자는 미확인입니다. 데이터 없음 또는 C=0이면 비율을 0%나 충족으로 확정하지 않습니다.
- `[판정]`은 등록 양식의 `전문가 비율 (N% 범위내/이내)` 문구에서 명시된 상한을 확인했을 때만 `N% 이내/초과`로 치환합니다. 반올림 전 비율로 비교하며 법령/RFP 준수를 단정하지 않습니다. 상한을 확인하지 못하면 검토 필요입니다. 배치기준 검토/요약·비율검토 문구 토큰도 연결했습니다.
- 모든 토큰은 선택 사항입니다. 토큰 없는 고정 제목·판정·문구는 그대로 두며, 실제 존재하는 미지원 토큰만 미치환 경고를 표시합니다. 장표 밖 참고 객체, 이미지·관계 파일, 원본 글꼴·색상·문단·표 병합을 보존합니다.
- 4개 미만 단계의 남은 슬롯은 빈 값으로 치환합니다. 4개 초과는 연속 단계 행을 복제해 기존 단계 영역 높이에 배치하며 우측 비율 열의 세로 병합을 확장합니다. 글꼴 축소/페이지 추가 없이 모든 단계를 표시하고 배치 검토 경고를 남깁니다. 안전하게 확장할 수 없는 구조는 일부 단계만 누락하지 않고 실패합니다.
- `전문가비율막대_PB_높이연동대상` 도형과 `기준비율막대_30퍼센트`를 식별하고 `전문가비율라벨_PB`에 `[PB]`가 있을 때만 그래프를 연동합니다. 0~30% 구간은 기준 막대 높이에 비례해 하단을 고정하고 라벨을 함께 이동합니다. 소수 비율이 줄바꿈되지 않도록 연동 라벨 폭만 확보하고 원본 폰트는 유지합니다.
- 비율 미확인/30% 초과는 고정 축을 임의 수정하거나 30%로 잘라 그리지 않고 연동 막대를 숨깁니다. 표·라벨에 실제 비율 또는 미확인을 표시하며 보고서에 사유를 알립니다. 도형 이름/라벨 토큰이 없거나 중복이면 임의의 그림을 변경하지 않고 경고합니다.
- 제공된 3.7 플레이스홀더 양식으로 4단계·6단계·30% 초과·미입력 사례를 LibreOffice PDF로 렌더링하여 각 1장, 모든 단계/합계 표시 및 비율 라벨 줄바꿈 해소를 확인했습니다. 운영 등록 파일을 다시 조회하거나 DB/NAS에 접근하지 않았으며 Windows PowerPoint 직접 검수는 아닙니다.

## 활동별 일수·공수 계산

입력 순서는 `예비조사(pre) : 감리시행(audit) : 사후관리(post)`입니다.

- 활동별 MD: 감리원별 해당 값을 합산
- 활동별 일수: 동일 활동의 병행 수행을 전제로 감리원별 해당 값의 최댓값 사용
- 소계 일수: 예비조사 일수 + 감리시행 일수 + 사후관리 일수
- 소계 MD: 세 활동의 MD 합산
- 누락·음수·비숫자 입력은 확인 필요 표시. 빈 값이나 잘못된 값을 정상적인 0으로 확정하지 않음
- 별도 달력에 인원별 순차 근무가 정의되는 업무에는 이 병행 수행 규칙을 그대로 적용할 수 없으므로 추가 규칙 확정 필요

사용자 확인 사례: 1명이 `1:5:1`, 나머지 4명이 `0:5:1`이면:

| 활동 | 일수 | 공수 |
|---|---:|---:|
| 예비조사 | 1일 | 1MD |
| 감리시행 | 5일 | 25MD |
| 시정조치 결과 확인 | 1일 | 5MD |
| 소계 | 7일 | 31MD |

## 사진 조회 및 데이터

- NAS 사진은 이름 기반 일괄 조회, 요청당 로그인 한 번, 최대 3개 동시 다운로드
- 이름 조회 라우트 충돌 해결, ID=0 인력도 구분, TBD는 조회 제외
- 인증·권한·네트워크 오류와 파일/경로 없음을 구분, PNG 시그니처 검사
- 사용자가 NAS 권한 문제를 해결했다고 확인했습니다. 이번 사진 슬롯 수정은 브라우저 PPT 생성기에 한정하며 NAS 통신·설정·권한은 변경하지 않습니다.
- PostgreSQL에 인력·경력·사업·단계 배정·템플릿·규칙 저장. 신규 DB 마이그레이션 없음
- `DATABASE_URL`, `NAS_BASE_URL`, `NAS_USERNAME`, `NAS_PASSWORD`는 Railway Variables 또는 로컬 `.env`에서 관리하며 커밋하지 않습니다.
- 사용자에게서 확인된 운영 DB 스키마를 변경하거나 저장소의 구형 schema로 재생성하지 마세요.

## 사용 방법

1. 인력·사업 HTML 적재 후 사업 상세의 자동화 PPT 모달을 엽니다.
2. 추가 단계·사진 구성을 선택합니다. 3.6은 등록된 사용자 양식을 사용하고 수행 PM을 선택합니다. 공수·일수 비교 기준은 자동 적용됩니다.
3. 생성 버튼을 누르면 검토용 PPT가 바로 다운로드됩니다.
4. 생성 보고서와 실제 장표의 날짜·공수·참여 인력·배치를 확인합니다.
5. 원본 폰트를 유지하므로 데이터가 물리적 공간을 초과하면 배치 검토 경고가 표시됩니다. 조용히 글자를 줄이거나 데이터를 누락하지 않습니다.

## 본문 PPT 생성 보고서 화면

- 자동화 PPT 모달은 최대 1,120px, 화면 높이의 92%까지 사용합니다. 모바일(640px 이하)은 작은 여백과 세로 배치를 사용합니다.
- 등록된 메뉴의 `parent_id`/`children` 계층과 `menu_number`를 사용해 다·라·마·바 등의 상위 목차별로 결과를 구분합니다. 보고서의 항목 번호는 `라.3.7`, `마.3.6`처럼 표시하며 제목에 이미 있는 중복 번호는 표시에서만 제거합니다.
- 상위 목차는 접기/펼치기가 가능하고, 같은 생성 작업의 진행 갱신 중에도 접힌 상태를 유지합니다. 새 생성 작업에서는 기본 펼침 상태로 시작합니다.
- 상단에 처리 목차 수·생성 장수·생성됨/검토 필요/생성 실패/생성 중 개수를 표시합니다. 항목별 상태 배지와 장수, 상위 목차별 실패/검토 개수를 표시합니다.
- 경고는 항목 아래 별도 목록으로 한 줄씩 표시합니다. 중복 문구만 제거하고 미치환 토큰·오류 원문을 잘라내지 않습니다. 공통 안내는 마지막 별도 영역에 표시합니다. 텍스트는 `textContent`로 렌더링합니다.
- 단독 3.6 및 선택 목차 생성에도 같은 보고서 형식을 적용합니다. 번호/상위 목차가 없으면 임의 번호를 붙이지 않고 번호 미등록/기타 목차로 표시합니다.
- 보고서용 메타데이터·표시만 변경하며 원본 메뉴/템플릿·PPT 생성 로직·합본 순서·부분 다운로드 정책·DB/NAS는 변경하지 않습니다. 기존 생성 실패/미치환 문제를 이 UI 변경으로 해결한 것은 아닙니다.
- 로컬 Chromium에서 실제 Hono SSR(모의 DB) 모달에 예시 생성 로그를 넣어 1,440px 화면의 1,120px 모달, 390px/320px 모바일의 가로 넘침 없음, 긴 경고 줄바꿈, 키보드 접기/펼치기와 갱신 시 상태 보존을 확인했습니다. 운영 DB/NAS 및 외부 페이지 요청은 사용하지 않았습니다.

## 미완성 범위와 다음 단계

- 전체 38개 목차의 동적 작성은 미완성. 미등록 템플릿은 실패로 표시
- `MANPOWER_MD` 전용 양식·생성기 필요. 인력 실적표를 공수표로 대체하지 않음
- 조직도·총괄 요건·교육·회사 실적 및 RFP/증빙 규칙 확정 필요. 3.7 수치 비율 생성은 연결했으며 법적 전문가 인정 범위 등은 담당자 검토 필요
- 고정 수행기한·수행방안·단계별 고려사항 문구는 자동 재작성하지 않음
- 전문 분야 막대는 배정 단계들의 최초~최종 기간 표시. 중간 비참여 구간 세분화는 미구현
- 템플릿 Base64 일괄 로드 유지
- 고정 슬라이드 크기·고정 글꼴로 임의의 무한한 데이터를 담을 수는 없습니다. 현재 사업 배치는 검증했으며, 극단적으로 많은 단계/인력에는 원본 폰트와 한 장을 유지한 추가 배치 설계가 필요할 수 있습니다.
- 다음 검증: 실제 Windows PowerPoint에서 최종 배치 확인

## 개발 및 검증

```sh
npm ci
npm test
npm run build
```

99개 자동 테스트 통과 및 Node.js 빌드 성공. 최신 사용자 `(3)` 실양식의 개별 PM 토큰·자격번호 DB 우선/사업 fallback·실제 0/누락 구별·감리원 중복 제거·원문 키워드 순서/SSR 삽입 방지·분산 토큰·고정 문구/스타일/객체/미디어 보존·dispatcher 및 합본 검증 7개를 추가했습니다. 기존 검증: PM DB 요약(총괄 19건/전체 141건/6년 2개월 시험 사례), 역할 분류, 인력/사업 ID 검증, SELECT-only/NAS 미호출, 0건과 조회 실패 구별, 잘못된 인력 응답 거부, 새 3.6 문장·빨간색 11pt 강조·고정 판정·표 좌표·관계 보존·6단계·특수문자 및 SSR 자동 조회 안내를 포함합니다. 3.7 실제 양식 기반 단계별 MD/테스터 포함/추가 단계/합계/비율/양식 상한, 미입력·0·음수·원본 누락, 선택적 토큰·고정 문구·스타일·병합·장표 밖 객체 보존, 6단계 확장, 그래프 연동 및 합본 후 미디어 관계 보존을 포함합니다. 조치확인 단계별 참여자·post 합계(6/6/6MD) 및 전체 공수 경고 제외, 잘못된 배정값·목록 누락·미치환 경고 유지 검증을 포함합니다. 저장 요구 감리 일수의 공통/일정 치환, 사업별 숫자 문자열·소수·0, 단위 중복 방지, 공백·분산 런, 레이아웃/마스터 치환 및 원본 서식·고정 문구 보존, 누락·잘못된 값의 미치환 경고를 검증합니다. 3.6의 토큰 일부/전체 생략 허용, 고정 문구·판정 셀의 rich XML 보존, 존재하는 토큰만 치환하고 미지원 토큰만 경고하는 동작을 포함합니다. 생성 보고서의 목차 계층/전체 번호/중복 번호 처리, 목차별 상태·장수·개별 경고, HTML 삽입 방지, 접힘 상태 보존, 선택 생성의 계층 유지, 반응형 모달 SSR 검증을 포함합니다. 전체 인력 합계 151MD(감리원 120+전문가 20+테스터 11), 요구값 변경, 단계별 5/4/6일 미충족 및 5/5/6일 충족, 미입력/0일, 추가·보조 단계 범위, 구형 선택값 무시, SSR 선택창 제거 및 사업별 기준 표시 검증을 포함합니다. 3.6 실제 사용자 양식의 선택적 토큰·PM 미지정·상근 미확정·일수 해석·공수 오류·분야별 인원·원본 서식/관계/외부 참고 보존·합본·단독 다운로드·SSR 원본 누락 플래그·넘침 경고 검증을 포함합니다. 기존 테스트는 제안 상세 실제 Hono GET 응답에서 요구사항 3/5/151, 미입력·0·숫자 문자열 표시 검증, 수행방안 rich XML 동일성 및 분야 계층/단계별 이름 대응 검증, 본문 치환·합본, 사진 모의 응답, 원본 표시 폰트 유지, 단계 확장, 조치확인 4단계/18명 및 대상 없음, 월 단위 기간, 병행 일수·MD 및 7일/31MD 사례를 포함합니다. 자동 테스트는 운영 DB/NAS를 사용하지 않습니다. 추가 실적표 테스트는 합성 템플릿으로 감리원 5명/전문가 9명을 각각 3페이지 생성하고, 각 페이지 및 최종 합본에서 원본 이미지 바이너리·참조 ID·외부 링크·본문 치환 보존과 누락 관계 파일 오류를 검증합니다. 사용자 원본 PPT를 Windows PowerPoint에서 직접 열어 확인한 결과는 아닙니다.

별도 읽기 전용 렌더링 확인: 실제 등록 템플릿과 인명을 익명화한 현재 사업 데이터를 사용했습니다. 일정 계획 25행, 세부 일정 6단계, 조치확인 4단계/10명 결과가 각각 1페이지이며 LibreOffice PDF의 표시 텍스트가 장표 범위 안에 있음을 확인했습니다. 세부 일정의 1일/1MD, 5일/25MD, 1일/5MD, 7일/31MD 및 전체 총계 126MD도 PDF에서 확인했습니다. 이번 조치확인 수정본은 원본 수행방안 `txBody`/`tcPr` 동일성, 파란색 강조 및 글머리표 보존, 마지막 투입 시기 행 표시를 추가 확인했습니다. 표시 텍스트 최하단은 약 531.6pt로 540pt 장표 안입니다. 이는 Windows PowerPoint 자체의 검수와는 다릅니다.

사진 슬롯 검증: 합성 양식 회귀 테스트에 2/4/6/9인 다중 페이지, 빈 슬롯, 사진 없음/TBD, 공유 rId, 재번호매김 ID 충돌, 외부 예시 보존, 사진 누락·중복·DOM 순서 변경 및 최종 합본 후 사진 바이트 대조를 포함합니다. 별도로 3.1/3.2/3.3의 실제 등록 양식이 동일함을 확인하고, 반영된 소스로 16페이지·배정 슬롯 63개와 추가 안전성 12개 사례를 재검증했습니다. 이름·사진 연결, 위치·크기, 예시 그림 보존, 합본 후 관계 누락 검사에서 불일치가 없었습니다. 실제 인력 사진 대신 구분 가능한 시험 PNG를 사용했으며, Windows PowerPoint 직접 검수나 임의로 변형된 모든 신규 양식을 보장하는 검증은 아닙니다.

## 배포

- 기존 3.6 생성기(`b21ecde`)와 3.7 완성 양식 파일(`f949437`)은 사용자 승인 후 Astra main에 푸시했습니다. 템플릿 업로드는 사용자가 직접 수행합니다. Railway 배포 완료 확인은 하지 않았습니다.
- 공수·일수 자동 비교 기준(`b2d20eb`)과 생성 보고서 UI(`08df22a`), 3.6 선택적 토큰 처리(`aaf3860`), 요구 감리 일수 연결(`6eef8ae`), 조치확인 공수 경고 범위(`53ebb19`)는 사용자 승인 후 Astra main에 푸시했습니다. Railway 배포 완료 여부는 조회하지 않았습니다.
- 3.7 생성기 연결(`8b79561`) 및 완성본 기반 3.6 본문 개선/PM DB 자동 조회(`47ebb8c`)는 사용자 승인 후 Astra main에 푸시했습니다. Railway 배포 상태는 조회하지 않았으며 배포 완료는 미확인입니다.
- 최신 사용자 `(3)` 파일의 개별 토큰·자격번호·사업 키워드 연결은 로컬 검증·커밋 범위이며 별도 승인 전에는 GitHub에 푸시하지 않습니다. 운영 DB·NAS·등록 템플릿 파일은 변경하지 않습니다. 템플릿 등록은 사용자가 직접 수행합니다.
- Dockerfile이 TypeScript를 컴파일하고 정적 파일(3.6 다운로드용 양식 포함)을 `dist/static`에 복사합니다.
- GitHub 푸시가 Railway 자동 배포를 유발할 수 있습니다. 승인 전 푸시하지 않습니다.
- 원본 저장소 및 원본 production DB는 변경 대상이 아닙니다.
