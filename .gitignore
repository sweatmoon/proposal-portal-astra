# 빌드 산출물
# dist/ — Railway가 직접 dist를 사용하므로 git에 포함
node_modules/

# 환경변수 (절대 커밋 금지)
.env
.env.*
!.env.example

# Cloudflare (더 이상 사용 안 함)
.wrangler/
wrangler.jsonc
vite.config.ts
ecosystem.config.cjs

# 로그
*.log
npm-debug.log*
