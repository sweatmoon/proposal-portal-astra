# 환경변수 예시 — 실제값은 Railway 대시보드에서 설정
# Railway PostgreSQL 연결: 자동으로 DATABASE_URL 주입됨

# 로컬 개발용
DATABASE_URL=postgresql://postgres:password@localhost:5432/webapp

# 서버 포트 (Railway가 자동 지정, 기본 3000)
PORT=3000

# 환경
NODE_ENV=development
